# @package _global_
# Phase 1: 仅训练腿部，上半身冻结（hold），不施加外力扰动
defaults:
  - /task/sac/x2_walk_flat/mujoco
  - _self_

training:
  task_name: X2WalkFlat
  sim_backend: mujoco

env:
  upper_body_driver:
    mode: hold
  domain_rand:
    push_robots: false
